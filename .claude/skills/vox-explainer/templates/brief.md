---
workflow: general-video
flow: automation
storyboard: yes
message: "一句话主张——全片每帧都在为它服务（不是话题名，是立得住、可被反驳的主张）"
destination: youtube
aspect: 1920x1080
language: zh
length: content-driven
angle: concept-then-demo
audience: 描述到"能据此决定哪些词必须当场解释"
---

## Intent

（这段片子要让观众看完能做什么 / 记住哪一句。写 3–5 句。）

## Customizations

- 材料策略：哪些内容用真实截图、哪些用一比一重画的 mock（重画必须在下面注明字段与文案的真实出处）。
- 视觉语法：VOX —— 大字排版、手绘圈注、示意图、数据卡、颗粒质感。
- 音轨：旁白语言与配音方案；有无配乐；音效是标点还是垫床。

## Notes

- 内容范围与**事实出处**（具体到文件名：`README.zh.md` / `cordis.patch.yml` / 源码路径）。不凭印象。
- 术语纪律：面向 `<audience>` 时哪些词一旦出现就必须当场用一句人话解释。
- 时长：content-driven（由真实旁白时长决定，不设上限）。
- 结尾必须点到已知限制；不许把实验性能力说成稳定能力。

---

## 运行中记录（中途决策，写回以保续跑）

> **这一节是给接手者的交接面，阶段 ④ 之后必须回写。** 不要只留在对话里。
>
> 建议记录：
>
> - **实际时长**：`{{TOTAL}}s` / `{{FRAMES}}` 帧。若与前述估计不同，写清是哪一环导致的
>   （通常是真实语音时长覆盖了估算——槽位 = 旁白 + 呼吸）。
> - **配音**：引擎与版本、音色参考文件、seed、精度、采样参数、是否做响度归一化
>   （写具体值，例如 I=-16 LUFS / TP=-1.5）。
> - **配乐决策**：有无、为什么。若因缺凭据而无配乐，写清"缺的是哪个凭据"，
>   以及降级方案（例如稀疏音效做标点，不拿劣质合成垫底）。
> - **字幕决策**：做没做、为什么。若没做，写清做准需要什么（例如词级时间戳 / ASR 模型）。
> - **集成修复清单**：装配阶段由 Lead 而非帧作者做的修复，逐条写。
> - **旁白稿差异**：锁定稿与本文件的指导稿若有差异，写清哪一条、为什么。
