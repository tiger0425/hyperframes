# SCRIPT — {{TITLE}}

> **本文件是旁白的唯一事实来源。** 阶段③之后文案冻结：改一个字就必须重跑该条 TTS、
> 重算槽位、回写 `BRIEF.md`（见 `references/_contract.md` §2 时间闭环）。

**Voice:** （引擎 + 版本 + 音色参考文件的绝对路径）
**Voice settings:** （seed · 精度 · 设备 · 采样参数 · 响度归一化目标，例如 RMS 3500 或 I=-16 LUFS / TP=-1.5）
**Voice direction:** （语气总纲。例如：讲解向、清楚、略快；术语落地时放慢半拍；不用煽动语气。）
**Audio:** 每条已合成并入库（`.media/manifest.jsonl`），路径为 `.media/audio/voice/voice_0NN.wav`。

> 生成方式：（脚本路径）+（规格文件），一次加载模型批量产出全部条目。
> 总语音时长：（合计秒数）。成片槽位总长：（秒数 —— 由真实语音时长反推，不是估计）。

---

## Line 1 — {{该行角色}} (Frame 1)

**Time:** {{起}} – {{止}}s ｜ `voice_001.wav`
**Delivery:** （这一条怎么念。例如：直接、抛问题，不铺垫。／"绝对不能重发"最重。）

    （逐字正文。标点与停顿即 TTS 输入；段内不换行。）

## Line 2 — {{该行角色}} (Frame 2)

**Time:** {{起}} – {{止}}s ｜ `voice_002.wav`
**Delivery:** （…）

    （逐字正文。）

---

## 逐条自检

- [ ] 每条正文**逐字**确定，包括标点与停顿。
- [ ] 术语纪律已执行：面向 `{{AUDIENCE}}` 时，未解释的术语为零；首次出现的术语当场配一句人话解释。
- [ ] 行数与帧数一致（`{{FRAMES}}` 行），每行绑定一个 `voice_0NN.wav`。
- [ ] **每条 wav 已过语音判别**（`verify-film-audio.mjs`：CV ≥ 0.9、静音帧 25–48%）——
      这一步专门抓"引擎加载错权重所以产出杂音"（文件在、时长对、能播放，但内容是噪声）。
- [ ] Time 区间与 `index.html` 的槽位一致（由 `verify-timeline.mjs` 核对）。
